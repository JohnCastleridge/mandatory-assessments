\documentclass[11pt, a4paper]{article}

% --- Packages ---
\usepackage[utf8]{inputenc}
\usepackage{geometry}
\geometry{a4paper, margin=1in}
\usepackage{graphicx} % For including figures (PDF/SVG recommended)
\usepackage{amsmath}  % For math equations
\usepackage{hyperref} % For clickable links to code/references
\usepackage{listings} % For appending code snippets if needed
\usepackage{float}
\usepackage{xcolor}

% --- Setup for Code Listings ---
\lstset{
    basicstyle=\ttfamily\small,
    breaklines=true,
    backgroundcolor=\color{lightgray!10},
    frame=single,
    numbers=left,
    numberstyle=\tiny
}

\renewcommand{\thesubsection}{\alph{subsection})}

% --- Document Info ---
\title{IN3190 - Digital Signal Processing: Mandatory Project}
\author{Vegard Nyqvuist Borgeraas}
\date{Autumn 2026}

\begin{document}

\maketitle

% ---------------------------------------------------------
\section*{Task 1: The Overview}
\setcounter{subsection}{0}
% ---------------------------------------------------------

\subsection{} % 1a)
% Run script, parse dataset, include map plot
The provided dataset containing the HDF5 sensor time series was parsed. Figure \ref{fig:1a} displays the geographic map of the event location and the respective recording stations. 

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{figures/1a.pdf} 
    \caption{Geographic locations of the sensors and the Hunga Tonga volcano.}
    \label{fig:1a}
\end{figure}

\subsection{} % 1b)
Based on the station metadata, the calculated great circle distances from the Hunga Tonga event are as follows:
\begin{itemize}
    \item \textbf{Shortest distance:} 2137.71 km
    \item \textbf{Longest distance:} 17687.13 km
\end{itemize}

\subsection{} % 1c)
Figure \ref{fig:1c} displays all sorted distances between the event location and the 201 station locations.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{figures/1c.pdf}
    \caption{Sorted great circle distances from Hunga Tonga to all recording stations.}
    \label{fig:1c}
\end{figure}

% ---------------------------------------------------------
\section*{Task 2: Data Filtering}
\setcounter{subsection}{0}
% ---------------------------------------------------------

\subsection{} % 2a)
The three provided FIR filter impulse responses, $h_1[n]$, $h_2[n]$, and $h_3[n]$, are plotted in Figure \ref{fig:2a}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{figures/2a.pdf}
    \caption{Impulse responses of the three FIR filters.}
    \label{fig:2a}
\end{figure}

\subsection{} % 2b)
See \texttt{task\_2.py} for the implementation of the convolution function. The custom implementation is verified using \texttt{pytest} by directly comparing its output against NumPy's built-in \texttt{np.convolve} function.

\subsection{} % 2c)
See \texttt{task\_2.py} for the implementation of the DTFT function.

\subsection{} % 2d)
The absolute values of the frequency spectra, $|H_1(e^{j\omega})|$, $|H_2(e^{j\omega})|$, and $|H_3(e^{j\omega})|$, are plotted against the physical frequency $F$ in Figure \ref{fig:2d}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{figures/2d.pdf}
    \caption{Absolute frequency spectra of the three provided FIR filters.}
    \label{fig:2d}
\end{figure}

\subsection{} % 2e)
Based on the frequency spectra observed in Figure \ref{fig:2d}, the filters are classified as follows:
\begin{itemize}
    \item $h_1[n]$ is a lowpass filter.
    \item $h_2[n]$ is a bandpass filter.
    \item $h_3[n]$ is a highpass filter.
\end{itemize}

\subsection{} % 2f)
All 201 input time series were successfully filtered using the three respective impulse responses via the built-in convolution function in \texttt{numpy}. See \texttt{task\_2.py} for the implementation of the function that performs the filtering.

\subsection{} % 2g)
from \ref{fig:2d}, it is evident that the bandpass filter $h_2[n]$ effectively isolates the frequency range of interest between 0.01 and 2 Hz. Therefore I chose to use this filter for further analysis.

Figure \ref{fig:section_plot} shows the resulting section plot with time on the x-axis and distance from Hunga Tonga on the y-axis.

\begin{figure}[H]
    \centering
    % \includegraphics[width=1\textwidth]{figures/section_plot.pdf}
    \caption{Section plot of the filtered waveforms (0.01–2 Hz) demonstrating global wave propagation.}
    \label{fig:section_plot}
\end{figure}

% ---------------------------------------------------------
\section*{Task 3: Celerity Estimation for the Acoustic Wave}
\setcounter{subsection}{0}
% ---------------------------------------------------------

\subsection{} % 3a
A custom routine was developed to loop over all station signals. For each station, the raw and filtered traces were analyzed to manually pick the arrival time of the higher-frequency infrasound wave. The picked times were saved for subsequent analysis.

\subsection{} % 3b
Figure \ref{fig:celerity_fit} plots the estimated travel times against the event-to-station distances. Identified outliers (caused by noise or competing arrivals) are marked in [Insert Color, e.g., red] and were excluded from the linear regression.

\begin{figure}[H]
    \centering
    % \includegraphics[width=0.8\textwidth]{figures/celerity_fit.pdf}
    \caption{Linear fit of travel time versus distance for celerity estimation. Outliers are excluded from the fit.}
    \label{fig:celerity_fit}
\end{figure}

By fitting a straight line to the accepted distance/time data, the slope yielded an estimated celerity of \textbf{[Insert Value]} m/s. 

Comparing this result to the literature, this estimate aligns with typical celerities of infrasound waves as reported by Vergoz et al. (2022) and Matoza et al. (2022), which state [Insert brief comparison/typical values from the papers].

\end{document}